#!/usr/bin/env bash

outputs=$(swaymsg -t get_outputs | jq '.[] | select(.model == "U28E590") | .name' | wc -l)
if [[ $outputs -gt 1 ]]; then
	notify-send "disabling output 'DP-4'"
	swaymsg output "DP-4" disable
fi
